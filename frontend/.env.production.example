# Copy to frontend/.env.production, or configure this variable in Vercel.
# The production browser calls Node through the same Vercel origin. Keeping
# this value relative avoids cross-origin cookies and CORS in production.
VITE_API_BASE_URL=/api
